import { TitleDefinition, FrameDefinition } from '../types';

// Shop exclusive static titles (currently empty - can be expanded later)
export const STATIC_TITLES: TitleDefinition[] = [];

// Shop exclusive static frames (currently empty - can be expanded later)
export const STATIC_FRAMES: FrameDefinition[] = [];
